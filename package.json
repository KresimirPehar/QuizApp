{
    "scripts": {
        "client": "cd client && npm run start",
        "server": "cd server && nodemon server.js",
        "sass-watch": "cd client/src/styles && sass --watch scss/style.scss:css/style.css",
        "dev": "concurrently --kill-others-on-fail \"npm run sass-watch\" \"npm run client\" \"npm run server\""
    },
    "dependencies": {
        "axios": "^0.18.0",
        "body-parser": "^1.18.3",
        "cors": "^2.8.5",
        "express": "^4.16.4",
        "express-session": "^1.15.6",
        "mongodb": "^3.1.10",
        "mongoose": "^5.7.5",
        "redux-immutable-state-invariant": "^2.1.0"
    },
    "devDependencies": {
        "concurrently": "^4.1.0",
        "nodemon": "^1.18.7"
    }
}
